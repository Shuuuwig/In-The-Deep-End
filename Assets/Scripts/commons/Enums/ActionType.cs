using UnityEngine;

public enum ActionType
{
    Light,
    Heavy, // might need to be full class or SO for data like move type, target type(burst, single, all)/targetable num of units
    Piercing,
    Buff,
    Debuff,
}
